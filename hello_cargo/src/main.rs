fn main() {
    println!("Hello, Cargo World!");
    println!("Made a change to see it re-compile!");
    
    // you have two command options cargo build or cargo run. cargo run is better, it will compile the application
    // first if needed, or check for changes, then run it after compiling, saving you having to cd into the folder to run it manually.
}