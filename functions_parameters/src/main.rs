fn main() 
{
    first_function(5); // we define our parameter which is 5
}

fn first_function(x: i32) // we then define it's type
{
    println!("The value of x is: {x}"); // and now we display 5 to screen
}